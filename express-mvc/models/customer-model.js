const findCustomer = () => {
  // const customers = SELECT * FROM `customers`
  // return customers
};

const createCustomer = customerObj => {
  // const newCustomer = INSERT INTO customers (name, address) VALUES (customerObj.name, customerObj.address)
  // return newCustomer
};

module.exports = { findCustomer, createCustomer };
